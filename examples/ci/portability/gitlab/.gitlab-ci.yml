stages:
  - regress

gait_regress:
  stage: regress
  image: golang:1.26
  script:
    - bash scripts/ci_regress_contract.sh
  artifacts:
    when: always
    paths:
      - gait-out/adoption_regress/
